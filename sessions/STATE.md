# Состояние проекта

**Обновлено:** 2026-08-15, сессия 8 (завершена)

## Готово

- **Контекст и спецификация** — сессия 1. `SPEC.md` с правилами расчёта,
  состояниями, границами, списком неоднозначностей и критериями приёмки;
  Часть II `AGENTS.md` (стек, команды, 11 запретов, поводы остановиться,
  чек-лист доказательств, формат баг-репорта); `brief.md`.
- **Наивный прогон приложения** — сессия 2. Opus 4.8, изолированная папка,
  один промпт. Результат в `app/`, коммит `882ddef`, SHA-1 сверены.
  Разобраны семь умолчаний `brief.md`.
- **Наивное тестирование** — сессия 3, первый из двух сравниваемых прогонов.
  Ответ сохранён дословно. 12 областей, 0 багов, 3 наблюдения.
- **Тест-план и прогон по спецификации** — сессия 4. 12 проверок:
  8 пройдено, 4 провалено, 2 заблокировано. Четыре баг-репорта,
  доказательства в `qa/evidence/`: три разбора разметки, один скриншот.
- **Автотесты** — сессия 5. `@playwright/test` 1.62.1, Page Object Model,
  четыре теста по одному на дефект, все красные.
- **Фикс BUG-03 и ретест** — сессия 6. Одна строка в `app/index.html`.
  Автотесты: **1 passed, 3 failed**. Полный ретест чек-листа:
  **9 пройдено, 3 провалено, 3 заблокировано**, сломалось попутно ничего.
  ЗБ-03 подтверждён разбором формулы.
- **Аудит** — сессия 7. Чистая сессия, Opus 4.8, папка `sessions/` закрыта
  целиком. Восемь находок, отклонённых нет. Две закрыты правками: ТП-13
  на правило «доставка считается до скидки» и повышение серьёзности BUG-01
  до «высокой». Шесть — незаписанные границы работы, идут в `README.md`.
- **Отчёт** — сессия 8. `README.md` переписан под сдачу: 11 разделов, включая
  «что не сделано», «что не покрыто», допущения и сравнение прогонов в цифрах.
  Инструкция запуска проверена реальным прогоном всех пяти шагов.
- **Git:** 19 коммитов, локальный HEAD совпадает с `origin/main`.

## В работе

Ничего. Сессия 8 закрыта.

## Не начато

Все этапы задания закрыты. Осталась финальная сверка перед отправкой:
прогнать чек-лист из `homework.pdf` и проверить, что ссылка на репозиторий
открывается со стороны (режим инкогнито либо доступ выдан `@gfarhad93`).

## Активные инструменты

- Claude Code, Opus 5 — рабочие сессии. Режим: спрашивать перед изменениями.
- Claude Code, Opus 4.8 — наивные прогоны (приложение и тестирование), авторежим.
- Playwright MCP v0.0.79 — интерактивная проверка страницы.
- `@playwright/test` v1.62.1 + Chromium — `npx playwright test`, 4 теста.
- Python 3 `http.server` на 8765, поднимается конфигурацией Playwright
  (`reuseExistingServer`) или вручную из корня проекта.
- git + GitHub.

## Известные проблемы

- **Три дефекта остались неисправленными намеренно:** BUG-01 (потолок 999
  вместо 99), BUG-02 (нормализация промокода), BUG-04 (промокод не снимается
  на пустой корзине). Разрешение на правку `app/` выдавалось точечно,
  только для BUG-03. Их красные тесты — доказательство точечности фикса.
- **Три проверки заблокированы данными каталога:** точка ровно 2000,00 ₽,
  округление копеек, равенство скидки и суммы товаров. Общая причина одна —
  восемь цен, кратных 50 копейкам, выбранных наивным прогоном произвольно.
  Тестопригодность данных закладывается вместе с данными; менять
  `app/products.json` нельзя.
- **ЗБ-03 подтверждён разбором формулы, а не тестом.** Разбор проверяет формулу,
  переписанную в тестовый код, и не заметит изменения строки в приложении.
- **Скидка при корзине дешевле 1 ₽** теоретически стала бы отрицательной
  (`subtotal - 100` уходит в минус). На текущем каталоге недостижимо:
  минимальная цена 135,00 ₽. Наблюдение, не дефект.
- **Сравнение прогонов ослаблено сменой модели:** наивные прогоны на Opus 4.8,
  рабочие сессии на Opus 5.
- **Второй реализации приложения не будет** — расхождение между таблицей цикла
  в `homework.pdf` и заданием 4 (QA), взято второе прочтение.
- **Порт 8765 дважды оказывался занят чужим процессом.** Перед проверками
  смотреть `cwd`: `lsof -a -p <pid> -d cwd`.
- **`README.md` пока описывает домашнее задание**, а не приложение.
- **Стенд раздаёт наружу весь корень** — включая `SPEC.md` и журнал.
  Отметить в `README.md`.
- Исходный шаблон `sessions/session-1.md` потерян — перезаписан до `git init`.

## Следующий шаг

`README.md` под сдачу. Обязательные разделы: как поднять стенд и открыть
приложение (строго по инструкции, без «допишите тут своё»), что сделано,
что не сделано, принятые допущения, сравнение двух прогонов в цифрах.

Отдельным разделом — **«что не покрыто»**: шесть находок аудита, которые
приняты, но не закрываются, потому что расширяют объём против плана.
Незаписанная граница неотличима от недосмотра, поэтому они идут в сдачу
списком, а не молчанием:

1. Тест `bug-03` остался бы зелёным на неверном фиксе через `>` — граница
   равенства держится на разборе формулы (ЗБ-03), а не на тесте.
2. Раздел `SPEC.md` «Чего точно не делаем» не проверяется ни одной проверкой.
3. Испорченный `localStorage` не проверен (ТП-12 покрывает только битый
   `products.json`).
4. Нечисловой и дробный ввод количества не зафиксирован даже как наблюдение.
5. Итог 0 ₽ на пустой корзине не закрыт прямым ассертом.
6. Пустая корзина на первом заходе проверяется только косвенно.

Плюс не забыть: стенд раздаёт наружу весь корень, включая `SPEC.md` и журнал.
